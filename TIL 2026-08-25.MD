6-1강. 계산 그래프와 Chain Rule

4.아주 작은 계산 그래프 직접가기

import tourch

#  w는 우리가 학습시키고 싶은 값이라소 가정합니다.
#  requires_grad=True를 주면 PyTorch가 w와  관련된 연산을 추적합니다.

6-2강.requiires_grad와 Tensor gradient

6-3강.loss.backward()와 .grad 확인

6-4강.

6-5강.

7-1강. Dataset과 DataLoader 역할

1. 이번에 우리가 배울 것

Dataset : 샘플과 label을 어떻게 보관하고 꺼낼지 정의합니다.
DataLoader: Dataset에서 샘플을 여러 개 묶어 batch로 만들어 줍니다.

Dataset -> DataLoader -> Batch 흐름
Dataset(샘플과 label 저장) -> DataLoader(batch 생성) -> for batch(in loader) -> model(학습)

2. 왜 데이터 파이프라인이 필요한가요?

딥러닝 학습 코드는 크게 세 부분을 나눌 수 있습니다.
구성 요소 / 역할
모델 / 입력을 에측값으로 변환합니다.
학습 step / loss, backward, optimizer update를 수행합니다.
데이터 파이프라인 / 학습 step에 batch를 공급합니다.

데이터 파이프라인이 없으면 학습 코드 안에 데이터 전처리, 파일열기, bath 생성, shulffle 로직이 모두 섞이게 됩니다. 그러면 코드가 길어지고 디버깅이 어려워집니다.

★중요
Dataset과 DataLoader는 학습 코드의 가독성과 재사용성을 높여주는 핵심 도구입니다

3. Dataset의 역할

Daraset의 "데이터 하나를 어떻게 꺼낼 것이가"를 담당합니다.

Dataset은 보통 다음 질문에 답합니다.

전체 데이터는 몇개인가요?
idx번째 샘플은 무엇인가요?
idx번째 샘플의 label은 무엇인가여?
필요한 전처리는 어디서 적용하나요?

PyTorch에서 Dataset은 보통 다음 두 메서드를 가집니다.

메서드 / 역할
__len__ / 전체 샘플 개수를 반환합니다.
__getitem__ / 특정 index의 샘플과 label을 반환합니다.

이번 강에서는 먼저 가장 간단한 TensorDataset으로 시작합니다.

4. DataLpader의 역할

Dataloader는 Dataset을 감싸서 batch 단위로 꺼낼 수 있게 해줍니다.

DataLoader가 담당하는 대표 기능은 다음과 같습니다.

기능 / 설명
batching / 여러 샘플을 하나의 batch로 묶습니다.
shuffling / 학습 데이터를 섞어 모델이 순서에 과하게 의존하지 않게 합니다
iteration / for batch in loader 형태로 반복할 수 있게 합니다.
parallel loading / num_workers를 통해 데이터를 병렬로 읽을 수 있습니다.

처음에는 batch_size와 shuffle부터 정확히 이해하면 충분합니다.

5. Tensor 데이터로 DataLoader 만들기

6. 첫 번째 batch 확인하기

-튜터 한마디
DataLoder를 처음 만들거나 전처리를 바꾼 직후에는 next(iter(loader))로 batch 하나를 꺼내 shape를 확인하세요. 내 epoch마다 출력할 필요는 없지만, 구현하거나 변경한 시점에 한 번 점검하는 습관만으로도 학습 루프 오류를 많이 줄일 수 있습니다.

7. 학습 루프와 연결하기


연습문제 꼭! 풀기

[읽기용 코드 조각]
x = torch.randn(32, 10)
y = torch.tensor([0, 1] * 16, dtype=torch.long)

logits = model(x)

import torch
from torch.utils.data import TensorDataset, DataLoader

# 샘플 20개, feature 4개입니다.
X = torch.randn(20, 4)

# 각 샘플의 class label입니다.
y = torch.tensor([0, 1] * 10, dtype=torch.long)

print("X shape:", X.shape)
print("y shape:", y.shape)

# TensorDataset은 여러 Tensor를 첫 번째 차원 기준으로 묶습니다.
dataset = TensorDataset(X, y)

print("dataset length:", len(dataset))

# 0번째 샘플을 꺼내봅니다.
sample_x, sample_y = dataset[0]

print("sample_x shape:", sample_x.shape)
print("sample_y:", sample_y)

# dataset[0]은 샘플 하나를  반환하므로 sample_x의 shape는 (4,)입니다.

loader = DataLoader(
    dataset,
    batch_size=5,
    shuffle=True
)

# 첫 번째 batch를 꺼냅니다.
batch_x, batch_y = next(iter(loader))

print("batch_x shape:", batch_x.shape)
print("batch_y shape:", batch_y.shape)
print("batch_y:", batch_y)

# 7. 학습 루프와 연결하기

import torch.nn as nn
import torch.optim as optim

model = nn.Linear(4, 2)
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

for batch_x, batch_y in loader:
  optimizer.zero_grad()

  # batch_x shape: (batch_size, 4)
  # logits shape : (batch_size, 2)
  logits = model(batch_x)

  # batch_y shape: (batch_size,)
  loss = criterion(logits, batch_y)

  loss = criterion(logits, batch_y)

  loss.backward()
  optimizer.step()

  print("loss:", loss.item())
  break # 예시에서는 첫 batch만 실행합니다.