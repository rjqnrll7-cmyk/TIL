# 진도&실습
- 진도: 딥러닝 심화 1장 1강 ~ 2장 5강
- 실습:

# __init__
- 
- 

# 검색
- 
- 

# 주의&비교
- 
- 

# 느낀점
- 

# 데일리 퀘스트 오답노트
- 

# 1-1강: RNN/LSTM의 한계와 Transformer 필요성

목차
- 1. RNN의 계산 계약
- 2. 장기 의존성과 gradient
- 3. LSTM이 바꾼 것과 바꾸지 못한 것
- 4. Attention에서 Transforner로
- 5. 구조를 공정하게 비교하는 법

1. RNN은 이전 상태를 기다립니다
- 시퀀스 입력 `x_t`는 이전 hidden state `h_{t-1}`와 함께 현재 상태를 만듭니다.
```
h_t =tanh(x_t W_x + h_(t-1) W_h + b)
x_1 → h_1 → h_2 → ... → h_L
```
`h_t`가`h_{t-1}`에 의존하므로 같은 시퀀스의 위치`t-1`은 위치 `t`계산이 끝나야 진행할 수 있습니다. 배치 안의 샘플과 한 시정의 행렬 연산은 병렬화할 수 있지만 **시간축 의존성 자체**는 남습니다.
```
import torch

# batch_girst=True이므로 입력 축 순서는 [B, L, D_in]입니다.
rnn =  torch.nn.RNN(
  input_size=4, # token마다 들어오는 feature 수 D_in
  hidden_size=6, # 각 시점이 내보내는 hidden feature 수 H
  batch_first=True,
)

# 샘플 2개, 길이 5, 입력 feature 4인 작은 batch입니다.
x = torch.randn(2, 5, 4) # [B=2, L=5, D_in=4]
output, h_n = rnn(x)

# output은 다섯 시점 전체, h_n은 마지막 시점의 층.방향별 상태입니다.
print("output:", tuple(output.shape))
print("h_n:", tuple(h_n.shape)
```
대표출력:
```
output: (2, 5, 6)
h_n: (1, 2, 6)
```
`output`은 모든 시점의 표현이고,`h_n`은 층.방향별 마지막 상태입니다. 다층.양방향에서는`output[:, -1]`와`h_n[-1]`을  같은 뜻이라고 단정하면 안 됩니다.
\r\n
\r\n

2. 긴 문맥에서  세 문제가 겹칩니다.

- 2_-1. 장기 의존성
초반 단어가 마지막 에측에 중요하면 정보가 많은 recurrent step을 통과해야 합니다. 첫 위치에서 마지막 위치까지의 최소 경로는 대략 `L-1`입니다.
- 2-2. Vanishing/Exploding gradient
시간축 역전파에서는 여러 jacobian이 반복해서 곱해집니다. 작은 값이 큰 값이 곱해지면 폭주할 수 있습니다. Gradient clipping은 폭주를 완화하지만 장거리 관계를 자동으로 학습하게 해주지는 않습니다.
- 2-3. 고정 크기 상태의 정보 병목
초기 RNN Encoder-Decoder는 입력 전체를 마지막 상태 하나에 압축했습니다. 입력이 길어질수록 필요한 세부 정보가 희석될 수 있습니다. Transformer 이전의 Attention도 이 병목을 줄이기 위해 Endoder의 여러 상태를 직접 참고하도록 도입되었습니다.

3. LSTM은 gradient 흐름을 개선하지만 순차성은 유지합니다.
- LSTM은 cell state와 gate를 사용합니다.
- 구성 / 역할
- Forget gate / 이전 정보 중 버릴 비율 결정
- Input gate / 새 정보를 반영할 비율 결정
- Output gate / 현재 출력에 드러낼 정보 결정
- Cell state / 비교적 직접적인 정보 전달 경로
- LSTM은 vanila RNN보다 자기 정보를 잘 보조하지만,`t`가`t-1`을 기다리는 구조와 제한된 상태에 문맥을 축약하는 부담은 남습니다. 시계열·스트리밍·작은 모델에서는 여전히 좋은 baseline이 될 수 있습니다.

4. Transformer는 위치 상을 직접 연결합니다.
- Self-Attention은 token이 같은 시퀀스의 다른 token을 직접 참고하게 합니다.
```
RNN: 1 -> 2 -> 3 -> ... -> L 경로 O(L)
Self-Attention: 1 -------> L 한 층 경로 O(1)
```
한 층의 모든 위치를 큰 행렬 연산으로 계산하므로 학습 시 sequence 위치의 병렬화가 쉽습니다. 대신 기본 score 행렬은`(L, L)`이고 원소 수가`L**2`으로 증가합니다. 생성 시에는 새 token이 이전 token애 의존하므로 Transformer도 token별 순차 생성이 필요합니다.
```
# 길이가 커질 때 두 비용 지표가 어건 속도로 변하는지 비교합니다.
for length in (4, 16, 128):
    rnn_ path = length - 1 # 첫 위치에서 마지막 위치까지 거치는 recrrent edge 수
    score_elements = length ** 2 # head gkskdml (L, L) score 행렬 원소 수
    print(length, "RNN path=", rnn_path, "Attention scores=", scor_elemets)
```
대표출력:
```
4 RNN parh= 3 Attention scores= 16
16 RNN path= 15 Attention scores= 256
128 RNN path= 127 Attention scores= 16384
```
5. 비교표: 우열이 아니라 제약의 차이
- 관점 / RNN/LSTM / Transformer
- 같은 layer 안 정보 경로/ 위치 수에 따라 길어짐 /먼 위치도 직접 연결
- 학습 시 시간축 계산 / 순차 의존 / 행렬 단위 병렬 계산
- 긴 문맥 / 상태에 게 축약 / 모든 위치를 직접 비교 가능
- 중요 비용 / 긴 직렬 경로 / 기본 Attention의 `L**2`메모리·연산
- 유리할 수 있는 조건 / 스트리밍, 작은 모델, 짧은 시계열 / 대규모 학습, 긴 문맥 관계, 전이학습

- 오류·주의사항
- "RNN은 병렬 처리가 전현 불가능하다"가 아니라 한 시퀀스의 시간축 의존이 핵심입니다.
- LSTM이 장기 의존성을 완전히 해결한다고 표현하지 않습니다.
- Self-Attention의 짧은 경로와 `L**2`비용을 함께 설명합니다.
- 양방향 RNN 출력은 실시간 미래 예측처럼 미래 에측처럼 미래 입력을 볼 수 없는 문제에 그대로 적용할 수 없습니다.

- 이해도 점검
1. `(B, L, H)`인 RNN`output`과`(layer*direction, B,H)`인`h_n`의 의미는 무엇ㄱ인가요?
2. Gradient clipping이 해결하는 문제와 해결하지 못하는 문제는 무엇인가요?
3. 길이 128에서 첫 위치와 마지막 위치의 RNN 경로와 Self-Attention score 수는 얼마인가요?
4. Transformer 학습의 병렬성과 생성의 순차성이  모순이 아닌 이유를 설명하세요.
```
점검 정답
1. 전자는 각 시점 표현, 후자느 층·방향별 마지막 상태입니다.
2. 폭주하는 gradient 크기는 제한하지만 장거리 관계와 순차 계산을 없애지는 못합니다.
3. 경로는 127, score dnjsth tnsms 16,384입니다.
4. 학습에서는 정답 token들이 주어져 여러 위치 logits를 함께 계산할 수 있지만, 추론 생성에서는 다음 token이 직전까지 생선된 token에 의존합니다.
```
이번 강의 요약
- RNN은 hidden state를 시간 순서대로 전달해 순서를 반영하지만 긴 직렬 경로를 만듭니다.
- LSTM은 cell state와 gate로 gradient·기억 문제를 완화하지만 시간 축 순차성은 유지합니다.
- Self-Attention은 먼 위치를 한 층에서 직접 연결하고 학습 시 행렬 병렬화 를 가능하게 합니다.
- Transformer의 짧은 정보 경로는 기본 score 행렬의 `L**2`비용과 함께 평가해야 합니다.

# 딥러닝 심화 1-2강: 미니 LLM 워크플로우와 누적 산출물
```
학습목표
- raw text에서 모델 결과까지의 전체 흐름을 순서대로 설명합니다.
- 분류와 생성의 model head.출력. 평가.계약을 구분합니다.
- train.validation.test.inference의 역할을 구분합니다.
- 이 실습의 실행값과 해석을 하나의 재현 가능한 실험 기록으로 연결합니다.
```
```
선수지식 확인.dataset, model, loss, metric의 기본 뜻과 Python dictionary를 알고 있어야 합니다.
```
이 강의는 뒤에서 배울 API를 먼저 외우는 시간이 아닙니다. 한 샘플이 `raw text -> token ID -> model output -> metric 또는 생성 문자열`로 이동하는 길을 먼저 잡고, 분류와 생성에서 무엇이 달라지는지 확인합니다. 이후 실습에서는 같은 지도 위에 실행값을 하나씩 채워 넣게 됩니다.

 1. 한 줄 워크플로우
```
 문제 정의 -> 데이터 검사·split -> tokenize·collate -> model·dbjecive -> train -> validation으로 선택 -> test 1회 평가 -> 저장·재로드 -> 새 입력 inference -> error analysis -> 최종 실험 기록
```
- 이 순서는 도구 이름이 바귀어도 유지되는 실험 게약입니다. 먼저 "무엇을 예측하고 어떤 기준으로 성공이라 할지"를 정하고, 마지막에는 다른 사람이 같은 환경과 설정으로 재현할 수 잇게 기록합니다.
- 도식의 윗줄은 분류와 새성이 공유하는 입력 경로이고, 아랫줄은 학습·평가 추론에서 달라지는 행동입니다. 특히 `model forward`가 같아 보여도 학습에서는 loss와 parameter update가, 평가에서는 고정된 checkpoint와 checkpoint와 metric이, 추론에서는 실제 서비스 입력과 후처리가 필요합니다.
- Hugging Face 공식 LLM Course의 전체 NLP pipline 도식은 raw text가 token과 ID를 거쳐 Transformer와 task head의 예측으로 바귀는 구간을 보여 줍니다. 이 그림을 볼 때는 "pipeline 한줄이면 끝난다"보다, 각 화살표의 입력·출력 계약을 task별로 먼저 고정해야 한다는 점을 읽어내세요.

 2. Raw text가 모델 입력이 되는 과정
- `input_ids`:vocabulary의 정수 ID
- `attention_mask`:실제 token은 1, PAD 위치는 0인 대표적 입력 mask
- `labels`:tokenizer의 산출아니라 dataset의 분류 정답 또는 LM collartor·학습 objective가 만드는 학습 목표
- `logits`:정규화 전 점수
- `logs`:학습할 때 gradient를 만드는 목적 함수
- Tokenizer와 model은 같은 checkpoint 계열을 사용해야 합니다. ID는 tokenizer의 vocabulary에 종속되기 때문입니다.

 3. Task별 출력 계약
- Task / 대표 model class / logit shape / 대표 평가
- 문장 분류 / `AutoModelForSeqeunceClassification` / `[B, C]` / accurarcy, macro-F1
- Masked LM / `AutoModelForMaskedLM` / `[B, L, V]` / mask 위치 top-k
- Causal LM / `AutoModelForCausalLM` / `[B, L, V]` / 생성 품질, 지연, 안정성
- 같은`AutoTokenizer`API를 써도 출력 head와 평가 기준ㅇ은 다릅니다.
```
import torch

# 예제는 샘플 2개(B=2), 클래스 3개(C=3)의 분류 logitsㅇ입니다.
# Logit은 확률이 아닌 실수 점수이므로 합이 1일 필요가 없습니다.
logits = torch.tensor([-1.2, 2.7, 0.4], [1.8, 0.2, -0.5]])
probabilities = torch.softmax(logits, dim=-1) # 클래스 축 C에만 softmax
predictions = logits.argmax(dim=-1) # 각 샘플에서 가장 큼 클래스 ID

print("prob shape:", tuple(probabilities.shape))
print("predictions:", predictions.tolist())
print("row sum:", probabilities.sum(dim=1)) # 각 행이 약 1인지 확인
```
대표 출력:
```
prob shape: (2, 3)
prediction: [1, 0]
row sums: tensor([1.0000, 1.0000])
```
Softmax는 클래스 축 `dim=-1`에 적용합니다. 배치 축에 적용하면 서로 다른 샘플이 경쟁하는 잘못된 계산이 됩니다.

 4. 학습.평가.추론을 구분합니다.
- 단계/ 정답 label / 파라미터 갱신 / 목적
- Train / 있음 / 있음 / loss를 줄이도록 학습
- Validation / 있음 / 없음 / hyperparameter·check point 선택
- Test / 있음 / 없음 / 모든 선택 후 최종 일반화 성능 확인
- inference / 보통 없음 / 없음 / 새 입력에 실제 결과 생성

- test 결과를 보며 epoch나 설정을 다시 고르면 Test가 validation 역할을 하게 됩니다. 최종 실험 기록에는 test를 몇 번 확인했는지 남깁니다.

 5. 생성에서 forward와 `generate()`는 다릅니다.
- Causal LM의 forward 한 번은 각 위치의 다음-token logits를 계산합니다. `generate()`는 마지막 위치에서 token을 선택하고 입력에 붙이는 과정을 종료 조건까지 반복합니다.
```
forward: input -> [B, L, V] logits
generate: forward -> token 선택 -> append -> forward -> ... decode
```
-`max_new_tokens`는 새로 만들 token 수만 제한하고, `max_length`는 입력과 출력을 합친 전체 길이를 제한 합니다. 생성 설정은 9장에서 비교합니다.

 6. 재현 가능한 실행 기록
- 최소 기록 단위는 다음과 같습니다.
```
# 비교 실험마다 같은 key를 사용하면 누락을 빠르게 찾을 수 있습니다.
manifest = {
  "problem": "한국어 뉴스 제목 3분류",
  "dataset_version": "new-mini-vi",
  "split_seed": 42, # 같은 split을 다시 만들기 위한 seed
  "model_id": "monologg/koelectra-small-v3-discrimnator",
  "tokenizer_id": "monologg/koelectra-small-v3-discriminator",
  "max_length": 64, # 입력 길이 분포를 본 뒤 고른 절단 상한
  "selction_metric": "validation_amcro_f1",
  "test_policy": "best checkpoint 확정 후,
}
```
- seed만 남겨도 충분하지 않습니다. package version, daraset fingerprint, model revision, 실제 checkpoint 경로와 평가 코드도 함께 기록해야 합니다.

 7. 9장을 관통하는 실험 기록 구조
 |장|남길 실행 증빙|
 |-|-|
 |1|문제 정의·구조 선택 근거·실험 manifest|
 |2|token 예시·길이 분포·padding/truncation 정책|
 |3|Attention score/weight/context와 mask 검증|
 |4|block·mask·memory shape 표|
 |5|BERT/GPT objective·출력 비교|
 |6|Moel Card·license·저장/재로드 증빙|
 |7|split·metric·best checkpoint·test·오류분석|
 |8|prompt-only·PEFT·fine-tuned 비교 조건|
 |9|decoding·Chat Template 결과와 최종 결론|

 각 표와 로그에는 "실제 실행값"과 "해석"을 분리합니다. 예상값을 실행값처럼 쓰지 않습니다.

 오류·주의사항
 - `model_id`와 tokensizer의 checkpoint를 다르게 섞지 않습니다.
 - Accuracy와 loss를 같은 개념으로 설명하지 않습니다.
 - 마지막 checkpoint를 자동으로 best checkpoint라 가정하지 않습니다.
 - 생성 문자열이 자연스럽다는 이유로 사실성이 검증됐다고 결론 내리지 않습니다.

 이해도 점검
 1. 분류 logits`[8, 3]`의 두 축은 무엇인가요?
 2. Validation과 test를 나누는 이유는 무엇인가요?
 3. Model과 tokenizer를 함께 저장해야 하는 이유는 무엇인가요?
 4. `gernerate()`와 forward 한 번의 차이는 무엇인가요?