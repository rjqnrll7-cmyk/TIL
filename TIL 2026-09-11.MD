# TIL
- 진도: 딥러닝 실전 & 프롬프트 엔지니어링 5장 1, 2강, 6장 1, 2강, 7장 1, 2, 3강, 8장 1, 2, 3강, 9장 1, 2강
- 실습:

# 알게 된 개념
1. fwd-i-search: _?
-  `Ctrl + C` 또는 `ESC`: 현재 검색 상태를 취소하고 빠져나옵니다.

2. API 호출 실패: 401. 가이드의 오류 안내를 확인하세요
- API 키 또는 토큰 누락/오타 확인

3. Qwen 날짜 오류
- 모델의 지식 컷오프(훈련 데이터의 한계)
- 가상의 타임라인과 현실의 충돌(환각 현상) 

# 튜터님이 알려주신 코드!(해석해볼것)
from datetime import datetime, timezone, timedelta
from ollama import Client

MODEL = "qwen3:4b-instruct-2507-q4_K_M"
QUESTION = "오늘 날짜와 요청 시점의 시각을 알려주세요."

client = Client(host="http://127.0.0.1:11434", timeout=180)

# PC의 현재 시각을 한국 표준시로 읽습니다.
now = datetime.now(timezone(timedelta(hours=9)))

response = client.chat(
    model=MODEL,
    messages=[
        {
            "role": "system",
            "content": (
                f"요청 시점의 현재 시각은 {now:%Y-%m-%d %H:%M:%S} "
                "(한국 표준시, UTC+09:00)입니다. "
                "이는 실행 환경에서 제공한 실제 시각이며 가상의 설정이 아닙니다. "
                "오늘, 어제, 내일은 이 시각을 기준으로 해석하세요."
            ),
        },
        {"role": "user", "content": QUESTION},
    ],
    stream=False,
    options={"temperature": 0, "num_predict": 256},
)

print("Python이 전달한 시각:", now.isoformat(timespec="seconds"))
print("모델 답변:", response.message.content