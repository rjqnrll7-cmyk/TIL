# 진도&실습
- 진도: 딥러닝 심화 3장 1강 ~ 3장 5강
- 교안 복습: 딥러닝 심화 3장 1, 2, 3, 5강 
- 실습: 

# __init__
- 
- 

# 검색
- 데이터 계약, 
- Collator
- merged_context

# 주의&비교
- 
- 

# 느낀점
- 

# 데일리 퀘스트 오답노트


2번 문항

**Scaled Dot-Product Attention 공식 softmax(QK^T/rtsq(dk))V에서, score를 만든 뒤 Softmax를 적용하기 전에 rtsq(dk)로 나눈는 이유를 한 문장으로 서술하세요.**

<font color= "red">오답이에요</font>

차원 dk가 커지면 dot-product score도 커지기 쉬운데, 너무 큰 값을 Softmax에 넣으면 한 위치에 weight가 몰릴 수 있습니다. rtsq(dk)로 나누는 scaling은 순위를 바꾸는 것이 아니라 값의 크기만 완화해 학습을 안정적으로 만드는 역할을 합니다.

# 딥러닝(심화) 3-1강

1. 왜 토큰끼리 서로 참고해야 할까요?

1-1. 토크의  기본 표현과 문맥 표현

2. Attention은 무엇을 하는 방식인가요?

| 현재 토큰이 다른 토큰들을 살펴보고, 필요한 정보를 서로 다른



- 

3. Query, Key, Value를 정보 찾기로 이해하기

|역할|쉬운 질문|정보 찾기 비유|
|-|-|-|
|Query|||
|Key|||
|Value|||

3-1. Query: 지금 필요한 정보

3-2. Key: 비교할 때 사용하는 특징

4. 문장 예제로 Q/K/V역할 읽기

5. Self-Attention에서

5-1. 선택학습: Shape만 코드로 확인하기

**Projection**ㅇ이란 무엇인가요?

선택·6. 연습문제:

참고·7. 자주 생기는 오해

참고·8. 이해도 점검

# 3-2: Scale Dot-product Attention 계산

1. 네 단계로 보는 Attention 계산

2. Score matrix는 어떤 표인가요?

2-1. Score는 확률이 아닙니다.

3. Scaling은 왜 하나요?

4. Softmax는 무엇을  바꾸나요?

4-1. 왜 `dim=-1`인가요?

5. Shape 이야기로 전체 흐름 읽기

6. 연습 문제: 작은 Tensor로 계산 흐름 확인
```python
import math
import torch

# 결과를 다시 실행해도 동일하게 만들기 위해 seed를 고정합니다.
torch.manual_seed(42)
# 토큰 세 개를 각각 2차원  벡터로 표현한 아주 작은 예제입니다.
# B=1: 문자 한 개
# L=3: 토큰 세 개
# D=2: 각 토큰 표현 차원 두 개
q = torch.tensor(
  [[[1.0, 0.0], 
  [0.0, 1.0], 
  [1.0, 1.0]]], 
  dtype=torch.float32, 
)

# 이해를 단순하게하기 위해 k도 같으  갑슬 사용합니다.
# 실제 Transformer에서는 Q와 K가 서로 다른 projection을 통과합니다.
k = q.clone

```


참고·7.자주 생기는 오류

참고·8. 이해도 점검

9. Sccaling과 Mask를 수식·Shape로 설명하

#3-3강: Attention Weight와 Context Vector 해석

1. Attention weight는 참고 비율이입니다.

Attention weight는 한 Query가 여러 key를 얼마나 참고할지 나타냅니다.

2. Hearap을 한 행식 읽는 방법


2-1. 행과 열

2-2. 대각선이 진하면 무엇을 의미하나요?

3. Context vexctor는 Value의 가중합니다.

3-1. Key와 Value를 다시 구분하기

4. Context vector가 토큰 표현을 어떻게 바꾸나요?

5. Attention weight 해석 시 주의할 점

5-1. 유용한 관찰

5-2. 피해야 할 단정

선택·6. 연습 문제: Weight와 Context 계산

다음 코드는 이미 만들어진 Attention weight와 context Value를 개선합니다.

선택·7. 연습 문제: Heatmap 시각화

#3-4강: Tensor로 Self-Attention 미니 구현

1. 교육용 구현에서 단순화하는 것

2. 코드의 다섯 단계

3. 입력·출력 Shaper 게약

4. 연습 문제: Self-Attention 함수 구현

4-1. 함수 실행하기

5. Mask 맛보기

5-1. Mask와 함께 실행하기

6. 디버깅 점검 순서

참고·7. 공식 PyTorch API와의 연결

참고·8. 이해도 점검

9. 실제 구현으로 넘어갈 때 확인할 것

#3-5강: Multi_Head Attention Shape과 디버깅

1. 왜 여러 Head를 사용할까요?

2. D-model, num_heads, D_head

2-1. Q, K, V는 입력을 그대로 세 번 복사한 값이 아닙니다.


Self-Attention에서는 Q, K, V의 출발점이 같은 입력 `x`이지만, 실제 값은 서로 다른 학습 가능한 선형변화을 거쳐 만들어집니다.
```Python

X: [B,L,D_model]

Q = XW_Q
K = XW_K
V = XW_V

Q, K, V: 각각 [B,L,D_model]

```
`W_Q`, `W_K`, `W_V`는 사람이 미리 정한 규칙이 아니라 학습 중 역전파로 업데이트되는 파라미터입니다. 같은 토큰 표현`X`를 사용하거라도 각 투영 행렬이 서로 다르기 때문에 다음 역할에 맞는 표현을 따로 학습할 수 있습니다.

|값|질문|직관|
|-|-|-|
|Query|현재 위치가 무엇을 찾는가?|참고할 정보를 요청하는 표현|
|Key|각 위치가 어떤 정보를 가지고 있는가?|Query와 비교되는 색인 표현|
|Value|선택되었을 때 실제로 전달할 정보는 무엇인가?|Attention weight로 섞이는 내용 표현|

```
★ 중요

Self-Attentionㅇ에서 Q, K, V의 원본 입력은 같지만 투영 결과는 일반적으로 다릅니다. "입력을 똑같이 세 번 복사한다"가 아니라"같은 입력을 서로 다른 관점으로 선형변환한다"고 설명해야 정확합니다.
```

2-2. Head 분리는 선형 투영 다음에 이루어집니다.

일반적인 구현 흐름은 다음과 같습니다.

```Python
입력 X [B,L,D_model]
-> Q, k, V 선형 투영 [B,L,D_model]
-> Head 분리 [B,H,L,D_head]
-> Head별 Attention 계산
-> Head 결과 [B,h,L,D_head]
-> Head 결합 [B,L,D_model]
-> Output Projection W_O
-> 최종 출력 [B, L, D_model]
```
각 head는 전체 차원 중`D_head`만 사용하지만, 단순히 원본`X`를 앞부분·뒷부분으로 잘라 보는 것이 아닙니다. 먼저 학습된 projection을 적용한 뒤 head 축으로 재배열합니다.

2-3. Cocatenate 뒤의 W_O는 왜 필요한가요?

Head별 Context Vector를 이어 붙이면 다시 `[B,L,D_model]`이 됩니다. 하지만 이어 붙이기만 하면 각 head의 결과가 단순히 옆에 배치된 상태입니다. 마지막의 학습 가능한 `W_O`는 여러 head의 결과를 다시 섞어 다음 Suvlayer가 사용할 표현으로 바꿉니다.

```Pyrhon

Concat(head_1, ..., head_H): [B,L,D_model]
Output = Concat(...) W_Q: [B,LD_model]

```

`W_O`까지 포함해야 Multi-Head Attetion의 출력 투영이 관성됩니다. PyTorch 같은 라이브러리는 내부 최적화를 위해 `W_Q`, `W_K`, `W_V`를 하나의 큰 행렬로 묶어 저장할 수도 있지만, 개념적으로는 세 투영을 구분해서 이해하면 됩니다.

2-4. Tensor Shape으로 전체 흐름 확인하기

3. Head로 나누고 다시 합치기

3-1. 왜 Head 축을 앞으로 옮기나요?

4. PyTorch MultiheadAttention 입출력

5. 연습 문제: Head split/merge Shape 확인

6. 연습 문제: nn.MultiheadAttention 실행

6-1. `average_attn_weight=True`라면?

7. 대표 오류와 디버깅

오류1. 

오류2. 

오류3. 

참고·오류4. Head 역할을 고정해서 해석합니다.

참고·8. 이해도 점검

9. MHA·MQA·GQA의 Q/K/V Head 수 (선택학습)

Decoder-only LLM의 config에서는 Query head 수와 Key/Value head 수가 다를 수 있습니다.

|구조|Query head|Key/Value head|특징|
|-|-|-|-|
|MHA|`H_q`|`H_kv = H_q`|각 Query head가 별도 K/V head를 사용합니다.|
|MQA|`H_q`|`H_kv = 1`|모든 Query head가 하나의 K/V를 공유합니다.|
|GQA|`H_q`|`1 < H_kv < H_q`|Query head 그룹이 K/V head가 K/V head를 공유합니다.|

예를 들어 `num_attention_heads=32`, `num_key_value_heads=8`이면 Query head 네 개가 K/V head 하나를 공유합니다.

[이번 강의 요약]
- 선택 학습에서는 MHA·MQA·GQA의 Query와 K/V head 수 차이와 KV Cache 변화를 비교합니다.
- Multi-Head Attention은 같은 입력을 여러 head가 서로가 다른 관점으로 처리할 기회를 줍니다.
- `D_head = D_model / num_heads`이며 나머지가 0이어야 합니다.
- 입력 `[B,L,D_model]`은 `[B,H,L,D_head]`로 나뉘고 다시 `[B,L,D_model]`로 합쳐집니다.
- PyTorch`nn.MultiheadAttention`으로 output과 head별 weight shape을 관찰할 수 있습니다.
- 디버깅에서는 `D_model`, `num_heads`, `batch_first`, `average_attn_weights`를 먼저 확인합니다.
