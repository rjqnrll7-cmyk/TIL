# TIL
- 진도: 딥러닝 실전 & 프롬프트 엔지니어링 1장 1강 ~ 2강, 2장 1강 ~ 2강
- 실습:

# 알게 된 개념
- e(자연상수)
 1. 내가 이해한 개념:
 2. 실제 개념:

- 
 1. 내가 이해한 개념:
 2. 실제 개념:

# AI 활용
-

# 튜터님께 질문
- 천튜터님:
- 강튜터님: Prompt-tuning, LoRA

# 수강생 질문
-
-

# 데일리 퀘스트 오답 노트

#### **<font color = "red">오답</font> 2번 문항**

**BCE(Binary Cross Entropy)를 계산할 때 log(0)으로 인한 계산 오류(inf)를 피하기 위해 확률값을 특정 범위로 제한하는 NumPy 함수는 무엇인가요?**

<font color = "green">정답</font>

np.clip

<font color = "blue">해설</font>

모델이 정확히 0 또는 1을 출력하면 log(0)이 발생해 계산 inf가 될 수 있습니다. np.clip을 사용해 확률을 [epsilon, 1-epsilon] 범위로 제한하면 이론 수치 오류를 피할 수 있습니다. 교안에서는 이를 통해 BCE 함수 내부에서 safe_probs를 만듭니다.

#### **<font color = "red">오답</font> 4번 문항**

**Cross Entropy 계산에서 정답 클래스의 확률이 1에 가까워질수록(모델이 정답을 강하게 확신할수록) loss는 어떻게 변하나요? (증가/감소 중 선택해서 답하세요)**

<font color = "green">정답</funt>

감소

<font color = "blue">해설</funt>

정답 클래스의 확률이 높아질수록 그 확률이 log값(log-probability)은 0에 가까워지고, 여기에 마이너스를 붙인 loss(-log(p)) 또한 작아집니다. 반대로 확률이 작아질수록 loss는 커집니다. 이는 예측이 정답에 가까워질수록 loss가 작아진다는 학습 목표와 일치합니다.

#### **<font color = "red">오답</font> 5번 문항**

**reduction="none"으로 loss를 계산했을 때의 결과로 옳은 것은?**

<font color = "green">정답</funt>

샘플별 loss를 배치 단위로 줄이지 않고 그대로 유지합니다.

<font color = "blue">해설</funt>

reduction 방식에는 none, mean, sum이 있으며, none은 샘플별 loss를 배치 단위로 줄이지 않고 그대로 유지합니다. mean은 평균 하나로, sum은 합 하나로 줄이는 방식입니다. 교안에서는 mean과 sum이 gradient 크기에도 영향을 줄 수 있어 reuction 방식을 함께 확인해야 한다고 설명합니다.

# 1장: NumPy 기반 손실 함수와 logits/softmax 구현

## 1장 1강: logits/softmax와 수치 안정성

1. logit과 softmax는 무엇인가요?

2. logits에서 확률로 가는 큰 그림
- logits가 확률과 손실로 바뀌는 흐름
`모델의 원시 점수 logits`>`max 빼기 수치 안정화`>`exp·정규화 softmax`>`정답과 비교 loss`

```
입력 데이터
-> 모델의 마지막 선형층
-> logits [클래스 원시 점수]
-> softmax
-> probability [합이 1인 분포]
-> 정답과 비교
-> loss
```

3. softmax를 한 단계씩 계산하기

4. 수치 안정성이 필요한 이유
- 지수 함수는 입력이 조금만 켜져도 매우 빠르게 증가합니다.

5. stale softmax의 핵심 구성요소

6. 배치 입력과 axis 해석

7. log-softmax 출력 해석


## 1장 2강: MSE/BCE/Cross Entropy 구현과 검증

# 2장: 경사 기반 학습과 옵티마이저 미니 구현
## 2장 1강: Gradient Descent와 파라미터 갱신
## 2장 2강: SGD/Momentum/Adam 직관 비교
