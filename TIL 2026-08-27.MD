찾아본것
-config()
-torch.Generator()
-random split()
-worker_init_fn
- 

비교해볼것
-torch.randint()
-torch.randn()
-torch.arange()

데일리 퀘스트 오답
# 4.문항
PyTorch에서 저장된 checkpoint 파일(torch.load로 불러온 dictionary)로부터 모델의 파라미터를 실제로 모델 객체에 주입할 때 사용하는 메서드 이름은 무엇인가요?
-model.load_state_dict()는 torch.load()로 불러온 dictionary 안의 model_state_disct를 받아 모델 파라미터를 복원합니다. load_state_dict에 파일 경로 문자열을 직접 넣는 것은 대표적인 실수 사례로 언급됩니다.

9-1강. seed 고정과 재현성 요소

1. 이번에 우리가 배울 것

2. 재현성이란 무엇인가요?

3.결과가 달라지는 주요 원인
-GPU연산이란? 랜덤성이 생기는 이유? 실행할 때마다 조금씩 달라짐. 난수를 고정을 하더라도 병렬 계산으로 미세하게 달라짐
GPU 고정함수

# 4. seed 고정 함수 만들기

import random
import numpy as np
import torch

def set_seed(seed=42):
  """
  Python, NumPy, PyTorch의 random seed를 고정합니다.

  seed:
      실험을 재현하기 위해 사용할 정수값입니다.
  """

  # Python 기본 random 모듈의 seed를 고정합니다.
  random.seed(seed)

  # Numpy randon seed를 고정합니다.
  np.random.seed(seed)

  # PyTorch CPU random seed를 고정합니다.
  torch.manual_seed(seed)

  # CUDA GPU를 사용할 수 있다면 CUDA random seed도 고정합니다.
  if torch.cuda.is_available():
    torch.cuda.manual_seed(seed)
    torch.cuda.manual_seed_all(seed)

set_seed(42)

x1 = torch.randn(3)

set_seed(42)

x2 = torch.randn(3)

print(x1)
print(x2)
print(torch.allclose(x1, x2))

# 5. DataLaoder와 random split에서 seed 사용하기

from torch.utils.data import TensorDataset, DataLoader, random_split

# seed는 모델과 난수를 만들기 전에 고정합니다.
set_seed(42)

X = torch.randn(100, 4)
y = torch.randint(0, 3, size=(100,))
dataset = TensorDataset(X, y)

# 데이터 분할과 매 epoch의 shuffle은 서로 다른 generator로 관리합니다.
split_generator = torch.Generator().manual_seed(42)
train_dataset, valid_dataset = random_split(
    dataset,
    [80, 20],
    generator=split_generator,
)

loader_generator = torch.Generator().manual_seed(42)
train_loader = DataLoader(
    train_dataset,
    batch_size=16,
    shuffle=True,
    generator=loader_generator,
    num_workers=0,
)

first_batch_x, first_batch_y =next(iter(train_loader))
print(first_batch_x.shape, first_batch_y.shape)

6. 이 기초 과정에서 보장하는 재현성 범위
목적 / 필요한 상태 / 이 과정의 범위
- 처음부터 다시 실행 / seed, 데이터 분할, 코드와 환경 / 지원
- 일반적인 학습 재개 / 모델, optimizer, 완료 epoch, 기록 / 지원
-완료된 epoch 다음부터 정확히 재게 / 위 상태 = Python/Numpy/CUDA 난수 상태 + Dataloader generator 상태 / 9-3강과 9-4강에서 지원

★ 정확한 재개의 경계
한 epoch의 모든 batch 처리와 history 기록이 끝난 뒤 저장한 checkpoint에서, 다음 eporch를 시작하는 경우

7. 자주 하는 실수
-실수1. 모델을 만든 뒤 seed를 고정하는 경우
-실수2. random split generator를 빼먹는 경우
-실수3. 재개할 때 seed 값만 다시 지정하는 경우

# 8. 연습 문제
# 다음 조건을 만족하는 set_seed 함수를 작성하세요.
# 1. Python random seed를 고정합니다.
# 2. NumPy random seed를 고정합니다.
# 3. PyTorch CPU random seed를 고정합니다.
# 4. CUDA가 사용 가능하면 CUDA seed도 고정합니다.

import random
import numpy as np
import torch

def set_seed(seed=42):
  """
  실험 재현성을 높이기 위해 여러 random seed를 한 번에 고정 합니다..
  """

  # Python 기본 random 모듈의 seed를 고정합니다.
  random.seed(seed)

  # NumPy random.seed를 고정합니다.
  np.random.seed(seed)

  # Pytorch CPU 연산의 random seed를 고정합니다.
  torch.manual_seed(seed)

  # GPU를 사용할 있다면 CUDA random seed도 고정합니다.
  if torch.cuda.is_available():
    torch.cuda.manual_seed(seed)
    torch.cuda.manual_seed_all(seed)

set_seed(123)
print(torch.randn(3))

set_seed(123)
print(torch.randn(3))

9. 이번 강 정리

9-2강. logging 설계

1. 이번에 우리가 배울 것
-8장에서는 history dictionary에 metric을 저장했습니다.
하지만 notebook을 닫거나 런타인이 초기화되면 메모리에 있던 history는 사라집니다.
-그래서 중요한 실험 결과는 파일로 남겨야합니다.
-이번 강에서는 다음 두 가지 로그를 만듭니다.

로그 / 저장 파일 / 의미
config log / config.json / 어떤 설정으로 실험했는지 저장
metric log / metric.csv / epoch별 loss/accuracy 저장

-Logging 설계
config > epoch metric > checkpoint path > plots / report
-checkpoint path 학습 지점으로 돌아갈 수 있게 저장 하는곳

2. lgging이 필요한 이유

3. 무엇을 기록해야하나요?

# 4. config를 JSON으로 저장하기

import json
import platform
from pathlib import Path

import torch

def save_config(config, save_dir):
  """
  실험 성정 dictionary를 config.json 파일로 저장합니다.

  config:
      실험 설정을 담은 dictionary입니다.

5. epoch metric을 CVS로 저장하기

6. print log와 file log 함께 사용하기

7. 자주 하는 실수
-실수1.config를 저장하지 않는 경우
-실수2.CSVheader를 빼먹는 경우
-실수3.매 epoch마다 CSV를 새로 쓰는 경우

# 8. 연습 문제

def save_

9-3강. state_dic와 checkpoint 구성

1. 
-학습한 모델을 저장하려면 모델 객체 전체를 저장하는 것보다, 모델의 학습된 파리미터를 담은 state_dict를 저장하는 방식을 주로 사용합니다.
- model.state_dict > optimizer.state_dict > epoch / best > torch.save

2. state_dict한 무엇인가요?


3. model.state_dict 확인하기

4. optimizer.state_dict 확인하기

5. 추론용 저장과 학습 재개용 저장의 차이

6. checkpoint dictionary 만들기

7. checkpoint 저장 함수 만들기

9-4강. 저장/불러오기와 학습 재개

1. 이번에 우리가 배울 것

불러오기 > 모델.복원 > 다음 계산 > 학습 재개 

2. 모델 불러오기와 학습 재개의 차이

3. 학습 재개 흐름

4. 독립 실행 준비: 2 epoch checkpoint 만들기

5. checkpoint 불러오기

6. 모델.optimizer.재현성 상태 복원하기

7. 3 epoch부터 학습 재개하기

8. 저장 경로가 검증하기

9. 평가나 추론만 할때

10. 자주 발생하는 실수

11. 직접 확인해보기

12. 이번 강의 정리

9-5강. 실험 폴더 구조와 결과 관리

1. 이번에 우리가 배울 것

2. 왜 실험마다 폴더를 나누나요?

3. 기본 실험 폴더 구조

4. 새로운 run directory 만들기

5. config.json 저장하기

6. 실제 3 epoch 미니 실험 준비하기

7. metrics와 checkpoint를 함께 저장하기

8. 실제 history로 학습 곡선 저장하기

9. 생성된 파일 확인하기

10. 직접 확인해보기

11. 9장 전체 정리

10-1강. 학습 곡선 해석

1. 이번에 우리가 배울 것

2. 왜 학습 곡선을 봐야 하는요?

3. train loss와 validation loss

4. 좋은 학습 곡선의 기본 패턴

5. 위험 신호를 읽는 방법

6. eopch 로그 저장 구조 만들기

7. loss curve 시각화하기

8. accuaracy curve 시각화하기

9. 자주 하는 실수

10. 연습 문제

10-2 강. Overfitting과 Underfitting 진단

1. 이번에 우리가 배울 것

2. Underfitting이란 무엇인가요?

3. Overfitting이란 무엇인가요?

4. 좋은 fit은 어떤 상태인가요?

5. 학습 곡선으로 진단하기

6. 진단 함수 만들기

7. 대응 방법 정리

8. 자주하는 실수

9. 연습 문제

10. 이번 강 정리