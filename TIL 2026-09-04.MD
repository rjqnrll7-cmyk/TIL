# 진도&실습
- 진도: 딥러닝 심화 4장 1강 ~ 4장 6강
- 교안 복습: 딥러닝 심화 3장 1, 2, 3, 5강 
- 실습: 

# __init__
- torch.triu
- 

# 검색
-  
- 
- 

# 주의&비교
- 
- 

# 느낀점
- 

# 데일리 퀘스트 오답노트

1번 문항

**GPT형 Dechder-only 모델과 원래 Transformer의 Decoder를 비교할 때, Decoer-only 모델에 없는 구성 요소는 무엇인가요?**

<font color= "red">오답이에요</font>

Decoder-only 모델은 별도의 Encoder가 없어 Encoder memory를 참고하는 Cross-Attention을 사용하지 않습니다. Causal Self-Attention, FFN, Residual Connection은 Decoder-only에도 그대로 존재합니다.

2번 문항

**시퀀스 길이가 5일 때, Causal Mask에서 Query 위치 2는 몇 번까지의 Key 위치를 볼 수 있나요? (예: 0, 1, 2 형식으로 답하세요)

<font color= "red">오답이에요</font>

Causal Mask의 각 행은 해당 Query 위치가 참고할 수 있는 key 위치를 나타내며, 미래 위치느 차단되고 현재 위치까지는 허용됩니다. 따라서 Query 위치 2는 0, 1, 2까지의 Key 위치를 볼 수 있습니다.

---

# 딥러닝 심화 4-1강: Transformer 전체 구조 조감 

```Python
학습 목표
- 선택
```

```Python

```
튜터 한마디

```Python
목차
```
---
1. Tansfformer란 무엇인가요?

핵심

2. Self-Attention과 Transforsrmer는 어떻게 다른가요?

3. 원래 Transformer의 Encoder-Decoder 흐름

3-1. Encoder

3-2. Decoder

---

4. 전체 구조를 네 구간으로 나누기

선택·4-1. 원 논문의 Fiure1로 전체 구조 읽기

1단계. 입력과 출력 토큰을 벡터로 바꿉니다.

2단계. Encoder가 입력 전체를 문맥화합니다.

3단계. Decoder는 미래를가린 채 현재까지의 출력을 읽습니다.

4단계. Decoder가 Encoder의 정보를 Cross-Attention으로 참고합니다.

5단계. Linear와 Softmax가 다음 토큰 후보를 만듭니다.

5. Transformer 계열의 세 가지 구조

5-1. Encoder-only

5-2. Decoder-only

5-3. Encoder-Decoder

6. Hidden state와 logits 구분

문자 분류

Causal Language Modeling

선택·7. 연습 문제: PyTorch에서 전체 Shape 관찰

선택·8. 연습 문제: 문제에 맞는 구조 선택

참고·

# 4-3강: Encoder Block과 En

1. 

1-1. 

1-2. 

2. 

3. 

4. 

5. 

6. 

# 4-4강: Decoder-only Blodck과 Causal Mask

# 4-5강: FFN, Residual Connection, LayerNorm

```
학습목표
- 선택 학습에서는 LayerNorm·RMSNorm과 일반 FFN·SwiGLU의 흐름을 비교합니다.
- Position-wise FFN이 각 토큰 위치에 같은 MLP를 적용하는 구조임을 설명할 수 있습니다.
- FFN의`D -> D_ff -> D`차원 변화를 설명할 수 있습니다.
- Residual Connection이 원래 입력 경로를 남기는 이유와 shape 조건을 설명할 수 있습니다.
- layerNorm이 각 토큰의 마지막 hidden feature 축을 정규화하는 방식을 설명할 수 있습니다.
- Pre-Norm과 Post-Norm의 계산 순서를 직관적으로 구분할 수 있습니다.
- PyTorch로 FFN, Residual, LayerNprm의 전후 shape을 확인할 수 있습니다.
```
```
선수지식 확인
- nn.Linear, activation function, hidden dimension 개념을 알고 있습니다.
- 4-3강에서 Encoder Block에 Attention과 FFN이 포함된다는 점을 확습했습니다.
- Tensor 덧셈에는 서로 호환되는 shape이 필요하는 저믈 알고 있습니다.
```

튜터 한마디

Transformer를 Attention만으로 설명하면 절반만 이해한것 입니다. Attention은 토큰끼리 정보를 나누고, FFN은 각 토큰 표현을 가공합니다. Residual과 LayeNorm은 이 과정을 여러 틍 반복할 수 있도록 학습 경로를 정리합니다.

1. Position-Wise FFN
position-wise FFN
FFN은 각 토큰 위치에 같은 MLP를 독립적으로 적용합니다.

```
입력 [B,L,D]
-> Linear(D,D_ff)
-> Activation
-> Linear(D_ff,D)
-> 출력 [B,L,D]
```

Attention과 FFN 비교

|구성요소|주된 역할|
|Self-Attenion|토큰 사이의 정보를 섞습니다.|
|Position-wise FFN|각 토큰 벡터 내부의 feature를 변환합니다.|

왜 중간 차원을 키울까요?

`D_ff`를`D`보다 크게 두면 토큰 표현을 더  넓은 공간으로 확자한 뒤 비선형 변환을 적용할 수 있습니다.

```
예: D=768 -> D_ff=3072 -> d=768
```

2. Residual Connction

Residual Connection은 Sublayer 출력에 원래 입력을 더합니다.

```Python
Y = X + Sublayer(X)
```

왜 사용할까요?
- 원래 입력 정보를 우회 경로로 전달합니다.
- Sublayer는 전체 표현을 처음부터 다시 만들기보다 입력에 필요한 변화랴을 더할 수 있습니다.
- 깊은 모델에서 gradient가 전달되는 직접 경로를 제공합니다.

Shape 조건
```Python
X: [B,L,D]
Sublayer(X): [B,L,D]
```
두 Tensor의 shape이 같아야 위치별 원소를 더할 수 있습니다.
---
3. LayerNorm

LayerNorm 정규화 축


Transformer에서`LayerNorm(D)`는 일반적으로 각 토큰 벡터의 마지막 hidden feature 축을 기준으로 평균과 분산을 계산합니다.
```
입력: [B,L,D]
정규화 축: D
출력: [B,L,D]
```

BatchNorm과의 직관적 차이
- BatchNorm: 여러 샘플의 batch 통계를 활용하는 경우가 많습니다.
- LayerNorm: 각 샘플·각 토큰의 feature 축 안에서 정규화합니다.

LayerNorm은 정규화 뒤에 학습 가능한 scale과 bias를 적용할 수 있습니다. 따라서 단순히 "출력을 항상 평균 0,
분산 1로 고정한다"고만 이해하면 부족합니다.

4. Pre-Norm과 Post-Norm

pre-Norm과 Post-Norm

Post-Norm
```Python
Y = LayerNorm(X + Sublayer(X))
```
원래 Transtformer 논문의 대표 흐름입니다.

Pre-Norm
```Python
Y = X + Sublayer(LayerNorm(X))
```
현대의 많은 모델에서 사용하는 흐름입니다.

핵심

둘 중  하나가  모든 상황에서 항상 정답인 것은 아닙니다. 실제 모델의 config와 구현에서 `norm_first`,`pre_norm`, `post_norm`, `post_norm` 설정을 확인합니다.

선택·5. 연습 문제: Position-wise FFN 구현
```Python
from __future__ import annotations

import torch
from torch import Tensor, nn

class PositionWiseFFN(nn.Module):
  """각 토큰 위치에 같은 두 층 MLP를 적용합니다."""

  def __init__(
      self,
      d_medel: int,
      d_ff: int,
      dropout: float = 0.0,
  ) -> None:
      super().__init__()

      # 첫 linear는 마지막 hidden dimension을 D에서 D_ff로 확장합니다.
      self.linear_in = nn.Linear(
        in_features=d_model,
        out_features=d_ff,
      )

      # GELU는 Transformer 계열에서 널리 사용하는 비선형 activation 중 하나입니다.
      self.activation = nn.GELU()

      # 학습 시 일부 activation을 확률적으로 0으로 만드는 regularization입니다.
      self.dropout = nn.Dropout(dropout)

      # Residual 덧셈을 위해 마지막 차원을 다시 D로 줄입니다.
      self.linear_out = nn.Linear(
        in_features=d_ff,
        out_features=d_model,
      )

  def forward(self, x: Tensor) -> Tensor:
      """입력 [B,L,D]를 같은 shape의 새 토큰 표현으로 변화합니다."""

      # [B,L,D] -> [B,L,D_ff]
      hidden = self.linear_in(x)

      # shape은 유지되고 값에 비선형 변환이 적용됩니다.
      hidden = self.activation(hidden)

      # Dropout은 학습 모드에서만 확률적으로 값을 0으로 만듭니다.
      hidden = self.dropout(hidden)

      # [B,L,D_ff] -> [B,L,D]
      output = self.linear_out(hidden)

      # Residual과 더할 수 있는지 shape을 검증합니다.
      assert output.shape == x.shape

      return output

  # 실행 에시
  BATCH_SIZE = 2
  SEQUENCE_LENGTH = 5
  D_MODEL = 16
  D_FF = 64

  x = torch.randn(
    BATCH_SIZE,
    SEQUENCE_LENGTH,
    D_MODEL,
  )

  ffn = PositionWiseFFN(
    d_model=D_MODEL,
    d_ff=D_FF,
    dropout=0.0,
  )

  ffn_output = ffn(x)

  print("input:", x.shape)
  print("ffn_output:", ffn_output.shape)

  assert ffn_output.shape == x.shape
```
선택·6. 연습 문제: Residual과 LayerNorm 연결

아래는 Pre-Norm 방식으로 FFN Sublayer를 연결한 간단한  모듈입니다.
```
class PreNormFFNBlok(nn.Module):
    ""LayerNorm -> FFN ->