# TIL
- 진도: 7장 1강 ~ 8강
- 실습:

# AI 활용
- 

# 데일리 퀘스트 오답 노트
- 

# 7장: 텍스트 분류 Fine-tuning 실습

## 7-1강: 텍스트 분류 문제 정의와 데이터 점검
1. 텍스트 분류 문제 정의
- 무엇을 입력으로 받고, 어떤 라벨을 예측하며, 어떤 기준으로 성공을 판단할지

`입력`>`라벨`>`평가`>`리스트`

★ 중요
모델 성능이 낮을때 원인은 모델 구조가 아니라 문제 정의, 라벨 품질, split 정책, metric 선택

2. 문제 정의 5요소
3. 데이터셋 구조 확인
   1. 라이브러리 설치
   2. 공통 설정과 데이터 로드
4. (선택) Label schema와 imbalance 점검
Label imbalance 이해

`다수 클래스`>`소수 클래스`
 
5. 결측·중복·data leakage 후보 점검

`결측`>`중복`>`라벨 분포`>`Leakage`

## 7-2강: DatasetDict, Label Encoding, Split 고정
1. DatasetDict와 split의 역할
- DataserDict와 split

`train`>`validation`>`test`

★중요 Validation 성능을 계속 보면서 모델을 고르면 validation도 간접적으로 의사결정에 사용된 것입니다. 그래서 최종 보고에는 가능하면 test 성능을 남기는 것이 좋습니다.

2. Label Encoding과 메핑 저장
3. 재현 가능한 split 만들기

`seed 고정`>`stratify`>`기록`

4. (선택) split 누수 점검
5. (선택) 연습 문제
6. (참고) 정답 확인
7. 이번 강의 요약

##7-3강: Tokenization Mapping과 Data Colletor
1. 왜 map(tokenize)이 필요한가요?
- `Tokenizer`; 문자열을 token id로 바꿈
- `attenrion_mask`; 실제 token과 padding 위치를 구분
- `Dataset.map()`; 이 변환을 데이터섹 전체에 일관되게 적용하는 도구

- `Tokenizer mapping`; map(tokenizer)는 원본 텍스트 tokenizer를 적용해 모델 입력 칼럼을 만듭니다.

`원본 컬럼`>`tokenizer 함수`>`새 컬럼`

2. Tokenization 함수 만들기

3. Dataset.map()으로 전체 split 변화
- `Dynamic padding`; 배치 안에서 가장 긴 문장에 맞춰 padding 합니다.

`고정 padding`>`동적 padding`>`Data Collator`

4. Data Collator와 dynamic padding
- Data collator는 Dataset 샘플 여러 개를 받아 모델이 처리할 수 있는 batch Tensor로 묶습니다. 문장마다 길이가 다르기 때문에, collator는 같은 batch 안에서 길이를 맞추는 padding을 수행할 수 있습니다.

`Data Collator의 역할`; Dateset 샘플 여러 개를 모델이 받을 수 있는 batch로 묶습니다.

`샘플 목록`>`padding`>`batch Tendor`

5. batch shape 확인

코드 해설

`attention_mask`에서 1은 실제 token, 0은 padding 위치입니다. 학습 중 모델이 padding을 실제 문장 내용처럼 참고하지 않도록 mask가 필요합니다.

6. (선택) 연습 문제
7. (참고) 정답 확인
8. 이번 강의 요약

7-4강: Sequence Classification Model과 Label Mapping(선택)
- (스킵)

7-5강: compute_metrics와 accurracy/Macro-F1
1. Accuuracy만으로 충분하지 않은 이유
2. Precision, Recall, F1 직관
3. Macro-F1이 필요한 상황
- Macro-F1은 각 클래스별 F1을 계산한 뒤 단순 평균합니다. 따라서 샘플 수가 적은 클래스도 같은 비중으로 반영됩니다.

`Macro-F1 직관`; 각 클래스 F1을 계산한 뒤 단순 평균합니다.

`클래스별 F1`>`단순 평균`>`소수 클래스 반영`

※ 참고사항
Macro-F1이 항상 최종 목표라는 뜻은 아닙니다. 문제에 따라 특정 클래스 Recall을 더 중요하게 보거나, 비용기반 metric을 따로 설계할 수도 있습니다.
4. compuse_metrics 함수 작성
5. (선택) Confusion matrix로 오류 유형 보기
`Confusion Matrix 읽기`; 어떤 정답이 어떤 에측으로 헷갈리는지 행렬로 확인합니다.
`행`>`열`>`대각선`>`비대각선`
6. (선택) 연습 문제
7. (참고) 정답 확인
8. 이번 강의 요약
- 이번 강의 요약에서는 Accuacy, Precision, Recall, F1, Macro-F1의 의미를 학습했습니다. Trainer의 `compute_metrics`함수는 logits와 labels를 받아 metric dict를 반환합니다.
- 불균형 데이터에서는 Accuracy만 보지 말고 Macro-F1과 클래스별 리포트, confusion matrix를 함께 확인해야 합니다.
- 다음 강의에서는 이 metric을 Trainer 설정과 연결합니다.

7-6강: TraingArguments와 Trainer 설정
```
학습 목표
- 계약을 점검 
- TrainingArguments가 학습 조건과 재현성 기록을 담당한다는 점을 설명할 수 있습니다.
- Trainer에 필요한 model, dataset, tokenizer, data_collator, compute_metrics의 역할을 설명할 수 있습니다.
- logging, evaluation, checkpoint 저장 전략을 설정할 수 있습니다.
```
```
이 강에서 처음 등장하는 개념
- TrainingArguments: batch size, epoch, learning rate, evaluation, saving, logging 등 학습 실행 조건을 담는 설정 객체입니다.
- trainer: Transformer 모델 학습·평가·저장 루프를 표준화한 Hugging Face API입니다.
- Hyperparameter: learning rate, batch size, epoch처럼 사용자가 정하는 학습 설정값입니다.
- Chekpoint strategy: 학습 중 어느 시점에 모델 상태를 저장 정하는 규칙입니다.
- Logging: loss, metric, step 정보를 기록해 학습 과정을 추적하는 과정입니다.
```
1. TrainingAruments는 무엇을 기록하나요?

`TrainingArguments 설정`; 학습 실행 조건을 한 곳에 기록하고 trainer에 전달합니다.

`학습 길이`>`최적화`>`평가/저장`>`로그`

★ 중요 TrainingArguments는 단순 코드 옵션이 아니라 **실험 재현성 문서**입니다. 나중에 성능 차이가 생겼을 때 어떤 설정이 달랐는지 추적하는 기준이 됩니다.

2. Trainer는 어떤 부품을 연결하나요?

`tainer 구성요소`; Trainer는 모델·데이터·토크나이저·collator·metric을 하나의 학습 루프로 연결합니다.

`model`>`datasets`>`collator`>`metrics`

3. TrainingArguments 설정 코드
- 코드 해설; 오류 방어코드
4. trainer 생성 코드

`logging·evaluation·save`; 학습 중 관찰할 것과 저장할 것을 미리 정합니다.

`logging`>`evaluation`>`save`
- 코드 해설; Trainer는 내부적으로 batch 생성, forward pass, loss 계산, backward, optimizer step, evaluation

5. 설정 점검 체크리스트
6. (선택) 연습문제
7. (참고) 정답 확인
8. trainer 전에 모델·Label Mapping 확인
9. 이번 강의 요약
- TraingArguments는 학습 실행 조건을 기록하고, Trainer는 모델·데이터·collator·metric을 연결해 학습 루를  실행합니다.
- 설정값은 성능과 재현성에 직접 영향을 주므로 의미를 알고 작성해야 합니다.
- 다음 강의에서는 실제 Fine-tuning을 실행하고 평가 결과와 checkpoint를 관리합니다.

7-7강: Fine-tuning 실행·평가·Checkpoint 관리
```
학습 목표
- 학습 정체·과적합·NaN·OOM 증상을 log와 작은 데이터 overfit 시험으로 진단할 수 있습니다.
- Trainer로 Fine-tuning을 실행하고 evaluation 결과를 확인할 수 있습니다.
- checkpoint, best model, final mode의 차이를 설명할 수 있습니다.
- 학습 산출물을 저장하고 재로드할 때 필요한 파일을 설명할 수 있습니다.
```
```
이 강에서 처음 등장하는 개념
- fine-tuning: 사전학습 모델의 파라미터를 특정 데이터셋과 태스트에 맞게 추가 학습하는 과정입니다.
- Checkpoint: 학습 중 특정 step 또는 epoch 시점의 모델 상태르 저장한 산출물입니다.
- Best model: 검증 metric 기준으로 가장 좋다고 선택된 checkpoint의 모델입니다.
- final model: 추론, 제출, 공유를 위해 최종 저장한 모델과 tokenizer 묶음입니다.
- Overfitting: 학습 데이터에는 잘 맞지만 검증/테스트 데이터에서는 성능이 떨어지는 현상입니다.
```
1. Fine-tuning 실행 전 확인
`fine-tuning 실행 흐름`; 사전학습 모델을  데이터셋 label에 맞게 업데이트합니다.

`train()`>`evaluate()`>`chekpoint`>`best model`
- fine-tuning은 사전학습 모델을 특정 문제의 label  기준에 맞게 조정하는 과정입니다.


2. tainer.train() 실행
3. evalution 결과 확인
4. checkpoint와 best model 관리
5. 최종 모델과 tokenizer 저장
6. (선택) 연습 문제
7. (참고) 정답 확인
8. 학습이 실패할 때 진단 순서
9. 이번 강의 요약

7-8강: Error Analysis와 Fine-tuning 리포트