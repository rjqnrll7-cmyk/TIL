7-2강. TrensorDatase과 Custom Dataset

1.이번에 우리가 배울 것
-이번 강에서는 Custom Dataset을 직접 만들어봅니다.

-TensorDataset은 이미 Tensor 형태로 준비된 데이터에 매우 편리합니다. 하지만 실제 프로젝트에서는 데이터가 파일 경로, 이미지, 텍스트, CSV row, JSON object 형태로 있을 수 있습니다. 이때는 Dataset 클래스를 상속해 직접 데이터 접근 방식을 정의합니다.

-TensorDatase은 이미 준비된 Tensor를 묶고, Custom Dataset은 __len__과 __getitem__으로 데이터 접근 방식을 직접 정의합니다.

2. TensorDataset 다시 보기
-TensorDataset(X, y)는 내부적으로 첫 번째 차원을 기준으로 데이터를 묶습니다.(X[n]과 y[n]이 하나의 샘플)
-TensorDataset은 간단하지만, 데이터 로딩 로직을 세밀하게 제어하기는 어렵습니다.

# 2. TensorDataset 다시 보기

import torch
from torch.utils.data import TensorDataset

X = torch.randn(10, 4)
y = torch.randint(0, 2, size=(10, ), dtype=torch.long)

dataset = TensorDataset(X, y)

print("dataset length:", len(dataset))
print("first sample:", dataset[0])

3. Custom Dataset은 언제 필요한가요?
상황 / 에시
파일에서 데이터를 읽어야 함 / 이미지 파일 경로를 읽고 PIL image로 로드
샘플마다 전처리가 필요함 / 텍스트 tokenizatioin, 이미지 transform
label을 별도 규칙으로 만들어야 함 / 파일명에서 label 추출
데이터가 Tensor로 미리 올라와 있지 않음 / CSV row, JSON line, audio file
- 실전 프로젝트에서느 Custom Dataset을 자주 만납니다. 모델 코드보다 데이터 로딩 코드가 더 복잡해지는 경우도 많습니다. 그래서 Dataset 구조를 초반에 잘 잡아두면 이후 프로젝트가 훨씬 안정적입니다.

4. Dataset 클래스 기본 구조
메서드 / 반드시 해야 하는 일
__len__ / len(dataset) 호출 시 반환할 전체 샘플 수를 정의합니다.
__getitem__ / dataset[idx] 호츨 시 반환할 샘플을 정의합니다.

# 4. Dataset 클래스 기본 구조

from torch.utils.data import Dataset

class MyDataset(Datasrt):
  def __init__(self):
    # 데이터 목록, 파일 경로, label 등을 준비합니다.
    pass
  
  def __len__(self):
    # 전체 샘플 개수를 반환합니다.
    pass
  
  def __getitem__(self):
    # idx번째 샘플과 label을 반환합니다.
    pass

5. 표 데이터 Custom Dataset 만들기

import torch
from torch.utils.data import Dataset

class TabularDataset(Dataset):
  """
  feature Tensor와 label Tensor를 받아
  idx번째 샘플을 반환하는 간단한 표 데이터 Dataset입니다.
  """
  def __init__(self, features, labels):
    """
    features:
       shape가 (num_samples, num_features)인 Tensor입니다.
    labels:
       shape가 (num_samples,)인 class label Tensor입니다.
    """
    # 샘플 개수가 같은지 확인합니다.
    assert len(features) == len(labels), "features와 labels의 샘플 수가 다릅니다."

    self.features = features
    self.labels = labels

  def __len__(self):
    """
    Dataset 전체 샘플 수를 반환합니다.
    """
    return len(self.features)

  def __getitem__(self, idx):
    """
    idx번째 feature와 label을 반환합니다.
    """
    x = self.features[idx]
    y = self.labels[idx]
    return x, y

X = torch.randn(12, 5)
y = torch.randint(0, 3, size=(12,), dtype=torch.long)

dataset = TabularDataset(X, y)

print("dataset length:", len(dataset))

sample_x, sample_y = dataset[0]
print("sample_x shape:", sample_x.shape)
print("sample_y:", sample_y)


6. Dataset과 Dataloader 연결하기

# Dataset과 DataLoader 연결하기

from torch.utils.data import DataLoader

loader = DataLoader(
    dataset,
    batch_size=4,
    shuffle=True
)

batch_x, batch_y = next(iter(loader))

print("batch_x shape:", batch_x.shape)
print("batch_y shape:", batch_y.shape)
print("batch_y stype:", batch_y.dtype)

7. Dataset 반환 구조 디버깅하기

# 7. Dataset 반환 구조 디버깅하기

def inspect_dataset(dataset):
  """
  Dataset의 첫 번째 샘플 구조를 확인합니다.
  """
  print("dataset length:", len(dataset))

  sample = dataset[0]
  print("sample type:", type(sample))

  if isinstance(sample, tuple):
    for i, item in enumerate(sample):
      if torch.is_tensor(item):
        print(f"sample[{i}] shape:", item.shape)
        print(f"sample[{i}] dtype:", item.dtype)
      else:
        print(f"sample[{i}] type:", type(item))
  else:
    print("sample:", sample)

inspect_dataset(dataset)

8. 자주 하는 실수

9. 연습문제

